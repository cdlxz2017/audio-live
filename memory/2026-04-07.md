# 2026-04-07 Daily Log

## 天道系统项目状态（00:53 记录）

### 正在进行的任务
- **天罚管理模块**：开发中（已完成数据库表+admin-app路由+前端页面，但admin-app编译环境有问题，需要重新编译）

### 已完成功能
- ✅ 部门管理体系（5级层级，自动排序，功能职责描述）
- ✅ 天道成员模块（成员管理+多部门分配）
- ✅ 天罚管理（CRUD表+后端路由+前端UI）
- ✅ 仙籍→天道成员 重命名
- ✅ 天谕→天罚 重命名

### 当前阻塞问题
1. **admin-app 编译环境异常**：npm install/tsc 命令无输出（环境问题），需要手动在服务器重新编译
2. **前端 vite build 失败**：api/index.ts 中模板字符串 `${id}` 变成了全角符号（写入时损坏）

### 手动修复步骤（需要在服务器执行）
```bash
# 1. 修复前端 api 文件（全角引号→半角引号）
# 文件：~/projects/tiandao-system/admin-frontend/src/api/index.ts
# 第51-54行的 `/${id}` 变成全角，需要改回半角反引号

# 2. 重新编译 admin-app
cd ~/projects/tiandao-system/admin-app
npm install
npx tsc
pm2 restart tiandao-admin-app

# 3. 重新构建前端
cd ~/projects/tiandao-system/admin-frontend
npx vite build
# 重启 vite preview (port 3015)
```

### 天罚表结构（heavenly_punishment）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT | UUID主键 |
| name | TEXT | 天罚名称 |
| sort_order | INTEGER | 自动排序 |
| publish_time | TIMESTAMP | 发布时间 |
| occurrence_start | TIMESTAMP | 事件开始 |
| occurrence_end | TIMESTAMP | 事件结束 |
| region | TEXT | 区域 |
| implementation_form | TEXT | 实现形式（发生事件） |
| required_result | TEXT | 要求结果 |
| status | TEXT | pending/completed/not_completed |
| completed_at | TIMESTAMP | 完成时间 |
| created_at | TIMESTAMP | 创建时间 |

### 天罚管理前端（NoticesView.vue）
- 字段：名称、发布时间、事件时段（开始/结束）、区域（固定9选项）、实现形式、要求结果、状态
- 区域选项：东海市、南海域、西疆省、北域州、中天城、南瞻部洲、北俱芦洲、东胜神洲、西牛贺洲
- 状态：待完成(黄)/已完成(绿)/未完成(红)

### 服务访问地址
- admin-app: http://localhost:3013
- admin-frontend: http://localhost:3015
- member-service: http://localhost:3002
# 2026-04-07 Daily Log

## 天道系统项目状态（00:53 记录）

### 正在进行的任务
- **天罚管理模块**：开发中（已完成数据库表+admin-app路由+前端页面，但admin-app编译环境有问题，需要重新编译）

### 已完成功能
- ✅ 部门管理体系（5级层级，自动排序，功能职责描述）
- ✅ 天道成员模块（成员管理+多部门分配）
- ✅ 天罚管理（CRUD表+后端路由+前端UI）
- ✅ 仙籍→天道成员 重命名
- ✅ 天谕→天罚 重命名

### 当前阻塞问题
1. **admin-app 编译环境异常**：npm install/tsc 命令无输出（环境问题），需要手动在服务器重新编译
2. **前端 vite build 失败**：api/index.ts 中模板字符串 `${id}` 变成了全角符号（写入时损坏）

### 手动修复步骤（需要在服务器执行）
```bash
# 1. 修复前端 api 文件（全角引号→半角引号）
# 文件：~/projects/tiandao-system/admin-frontend/src/api/index.ts
# 第51-54行的 `/${id}` 变成全角，需要改回半角反引号

# 2. 重新编译 admin-app
cd ~/projects/tiandao-system/admin-app
npm install
npx tsc
pm2 restart tiandao-admin-app

# 3. 重新构建前端
cd ~/projects/tiandao-system/admin-frontend
npx vite build
# 重启 vite preview (port 3015)
```

### 天罚表结构（heavenly_punishment）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT | UUID主键 |
| name | TEXT | 天罚名称 |
| sort_order | INTEGER | 自动排序 |
| publish_time | TIMESTAMP | 发布时间 |
| occurrence_start | TIMESTAMP | 事件开始 |
| occurrence_end | TIMESTAMP | 事件结束 |
| region | TEXT | 区域 |
| implementation_form | TEXT | 实现形式（发生事件） |
| required_result | TEXT | 要求结果 |
| status | TEXT | pending/completed/not_completed |
| completed_at | TIMESTAMP | 完成时间 |
| created_at | TIMESTAMP | 创建时间 |

### 天罚管理前端（NoticesView.vue）
- 字段：名称、发布时间、事件时段（开始/结束）、区域（固定9选项）、实现形式、要求结果、状态
- 区域选项：东海市、南海域、西疆省、北域州、中天城、南瞻部洲、北俱芦洲、东胜神洲、西牛贺洲
- 状态：待完成(黄)/已完成(绿)/未完成(红)

### 服务访问地址
- admin-app: http://localhost:3013
- admin-frontend: http://localhost:3015
- member-service: http://localhost:3002

---

## 后续更新（01:46）

### 端口纠正
- 正确管理后台：**3015**（admin-frontend，Vue 原版）
- 3014 是误搭建的 Vite，已删除

### 天罚管理修复
- **BUG**：编辑天罚时状态无法保存（`form` 对象和 `openEdit` 函数都缺少 `status` 字段）
- **修复**：在 `form` 初始值和 `openEdit` 中加入 `status: row.status`
- 重新构建：`cd ~/projects/tiandao-system/admin-frontend && npx vite build`
- 前端已重启，运行在 3015

### 当前服务状态
| 端口 | 服务 |
|------|------|
| 3015 | ✅ admin-frontend（管理后台，含天罚管理） |
| 3013 | ✅ tiandao-admin-app 后端 API |
| 3004 | ✅ tiandao-auth |
| 3002 | ✅ tiandao-member |
| 3011 | ✅ tiandao-world-event-service |

### 用户确认的功能要求
- 区域字段：**自由填写**（不是固定9选项）
- 状态：pending / completed / cancelled（编辑时可改）
- 完成时间：`completed_at` 自动记录（已完成状态时）
# 2026-04-07 Daily Log

## 天道系统项目状态（00:53 记录）

### 正在进行的任务
- **天罚管理模块**：开发中（已完成数据库表+admin-app路由+前端页面，但admin-app编译环境有问题，需要重新编译）

### 已完成功能
- ✅ 部门管理体系（5级层级，自动排序，功能职责描述）
- ✅ 天道成员模块（成员管理+多部门分配）
- ✅ 天罚管理（CRUD表+后端路由+前端UI）
- ✅ 仙籍→天道成员 重命名
- ✅ 天谕→天罚 重命名

### 当前阻塞问题
1. **admin-app 编译环境异常**：npm install/tsc 命令无输出（环境问题），需要手动在服务器重新编译
2. **前端 vite build 失败**：api/index.ts 中模板字符串 `${id}` 变成了全角符号（写入时损坏）

### 手动修复步骤（需要在服务器执行）
```bash
# 1. 修复前端 api 文件（全角引号→半角引号）
# 文件：~/projects/tiandao-system/admin-frontend/src/api/index.ts
# 第51-54行的 `/${id}` 变成全角，需要改回半角反引号

# 2. 重新编译 admin-app
cd ~/projects/tiandao-system/admin-app
npm install
npx tsc
pm2 restart tiandao-admin-app

# 3. 重新构建前端
cd ~/projects/tiandao-system/admin-frontend
npx vite build
# 重启 vite preview (port 3015)
```

### 天罚表结构（heavenly_punishment）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT | UUID主键 |
| name | TEXT | 天罚名称 |
| sort_order | INTEGER | 自动排序 |
| publish_time | TIMESTAMP | 发布时间 |
| occurrence_start | TIMESTAMP | 事件开始 |
| occurrence_end | TIMESTAMP | 事件结束 |
| region | TEXT | 区域 |
| implementation_form | TEXT | 实现形式（发生事件） |
| required_result | TEXT | 要求结果 |
| status | TEXT | pending/completed/not_completed |
| completed_at | TIMESTAMP | 完成时间 |
| created_at | TIMESTAMP | 创建时间 |

### 天罚管理前端（NoticesView.vue）
- 字段：名称、发布时间、事件时段（开始/结束）、区域（固定9选项）、实现形式、要求结果、状态
- 区域选项：东海市、南海域、西疆省、北域州、中天城、南瞻部洲、北俱芦洲、东胜神洲、西牛贺洲
- 状态：待完成(黄)/已完成(绿)/未完成(红)

### 服务访问地址
- admin-app: http://localhost:3013
- admin-frontend: http://localhost:3015
- member-service: http://localhost:3002

---

## 后续更新（01:46）

### 端口纠正
- 正确管理后台：**3015**（admin-frontend，Vue 原版）
- 3014 是误搭建的 Vite，已删除

### 天罚管理修复
- **BUG**：编辑天罚时状态无法保存（`form` 对象和 `openEdit` 函数都缺少 `status` 字段）
- **修复**：在 `form` 初始值和 `openEdit` 中加入 `status: row.status`
- 重新构建：`cd ~/projects/tiandao-system/admin-frontend && npx vite build`
- 前端已重启，运行在 3015

### 当前服务状态
| 端口 | 服务 |
|------|------|
| 3015 | ✅ admin-frontend（管理后台，含天罚管理） |
| 3013 | ✅ tiandao-admin-app 后端 API |
| 3004 | ✅ tiandao-auth |
| 3002 | ✅ tiandao-member |
| 3011 | ✅ tiandao-world-event-service |

### 用户确认的功能要求
- 区域字段：**自由填写**（不是固定9选项）
- 状态：pending / completed / cancelled（编辑时可改）
- 完成时间：`completed_at` 自动记录（已完成状态时）

---

## 天道系统前端大修（02:09-02:54）

### 根本问题（已解决）
- API 响应拦截器 `response => response.data` 解包了 axios 响应
- 但页面代码访问 `res.data?.data`（部门）和 `res.data?.members`（成员），路径对不上
- 修复：拦截器改为 `response => response`，成员数据访问改为 `res.data?.data`
- `getMembers()` 调用路径从 `/members` 改为 `/admin/members`（正确的 admin-app 路由）

### 前端 UI 变更
- 删除模块：境界、功法、诸天
- 重命名：因果→因果系统，天罚管理→天罚系统，天象→天象系统
- 新增：记忆系统（占位页面，暂无功能）
- 侧边栏顺序：仪表盘 → 天道成员 → 记忆系统 → 天象系统 → 天罚系统 → 因果系统 → 设置
- 页面标题「天道成员管理」→「天道成员」
- 因果系统右侧展示区标题「因果系统」→「因果管理」
- 标记模式：顶部栏增加「🔘 标记 OFF/ON」按钮，点击后所有 data-label 元素显示橙色标签

### 数据库清理
- 删除 `deities` 表（前端未使用）
- 删除 `admin`（单数）空表
- 保留：`member`、`department`、`department_member`、`admins`、`consumed_events`
- 部门数据清空，顶层「天道」部门已建立

### 当前模型
- 已切换至 `deepseek/deepseek-reasoner`

### 关键文件路径
- 前端源码：`/home/ai/projects/tiandao-system/admin-frontend/src/`
- API 客户端：`src/api/index.ts`（baseURL: http://localhost:3013）
- 路由：`src/router/routes.ts`
- 成员页面：`src/views/members/MembersView.vue`
- 记忆系统占位：`src/views/memory/MemoryView.vue`


---

## 天道系统功能更新（03:00-07:07）

### 仪表盘大改版（04:00-04:45）
- 五大系统统计卡片：天道成员 / 记忆系统 / 天象系统 / 天罚系统 / 因果系统
- 天象系统卡片：显示新闻总数 + 最新地震（震级+中文地名+时间）
- 仪表盘服务状态：9个服务精简展示
- 标记模式：Ctrl+Shift+D 切换

### 记忆系统独立页面（04:47）
- 路径：`/memory`
- 三大板块：①服务状态 ②记忆条数 ③近7天动态
- 每1小时自动刷新
- 每个服务有功能描述

### 天象系统重构（05:13-05:32）
**数据库**：`tiandao_db.real_world_news`（手动创建，不走 Prisma）
- 7个分类：自然灾害、地缘政治、热点新闻、气候气象、全球政治、科技新闻、金融经济
- 抓取：Brave Search API 优先（`BSA4M_EWaBUh2b_llu3McB1DcnRYyAG`），失败降级 BBC RSS
- 新闻标题+摘要用 Google Translate API 翻译为中文
- 定时每4小时自动抓取
- 去重：SHA256(title+category)
**实时地震**：USGS API（natural_disaster子标签），地名自动翻译中文

### 现世报系统重构（06:06-07:07）
**数据库**：`tiandao_karma.instant_karma_record`（karma-service专用库）
- 修复：karma-service 之前连错 `tiandao_db`，已改回 `tiandao_karma`
- 表字段：`record_id`(主键), `member_id`, `karma_type`, `level`, `merit_delta`, `current_merit`, `status`, `manifestation`, `created_at`
- 规则表：`tiandao_karma.karma_rule`（含 sort_order, duration_days, severity_score, is_active）

**功能**：
- 规则管理：序号/类型/等级/因/果/报应时效/启用+停用（同时显示）
- 快速施报：成员姓名手动输入+类型+等级+因+果+报应时效滑块
- 施报记录：历史列表，可删除（用 `record_id` 主键）
- 删除接口：`/karma/records/:recordId`（在 karma.service.routes.ts）

**报应等级（业报）**：轻微7天/中度30天/重度60天/极重90天
**报应等级（福报）**：小祥7天/中祥30天/大祥90天

### 天罚系统（heavenly_punishment）
- `tiandao_db.heavenly_punishment` 表手动创建
- 前端路径：`/notices`

### 删除 technique-service
- 原因：9721次/20h重启，严重故障
- PM2 中移除，ecosystem.all.json 同步更新

### karma-service 数据库配置
- **正确配置**：`DATABASE_URL=postgresql://openclaw_ai:zyxrcy910128@localhost:5432/tiandao_karma`
- 注意：之前错误连到 `tiandao_db` 导致记录表结构不一致

### 服务端口（最新）
- 3015: admin-frontend（Vue 管理后台）
- 3013: admin-app（后端 API）
- 3002: member-service
- 3003: time-service
- 3004: auth-service
- 3005: underworld-service
- 3006: heaven-service
- 3007: karma-service
- 3010: notice-service
- 3011: world-event-service
- 3012: gateway

### 待修复/未完成
- 删除施报记录功能：接口已通（返回 ok:true），但记录 `id` 为 undefined 时不显示删除按钮
- karma-service 数据库切换后旧记录丢失（施报记录为0）

---

## 天道系统文档（08:15）

### 运维手册
- **路径**：`/home/ai/projects/tiandao-system/docs/TIANDAO-OPERATION-MANUAL.md`
- **内容**：系统架构/数据库/运维命令/故障处理/开发指南

### 关键文件
- PM2 配置：`/home/ai/projects/tiandao-system/ecosystem.all.json`
- karma 前端：`admin-frontend/src/views/karma/KarmaView.vue`
- karma 后端路由：`services/karma-service/src/routes/karma.routes.ts`
- karma schema：`services/karma-service/src/types/karma.types.ts`
- admin-app 路由：`admin-app/src/routes/karma.routes.ts`

### 数据库连接
- tiandao_db：PostgreSQL tiandao_db（成员/部门/天罚/新闻/时间状态）
- tiandao_karma：PostgreSQL tiandao_karma（现世报记录/规则）

### PM2 服务（11个）
member(3002)/time(3003)/auth(3004)/underworld(3005)/heaven(3006)/karma(3007)/notice(3010)/worldevent(3011)/gateway(3012)/admin-app(3013)/admin-frontend(3015)

