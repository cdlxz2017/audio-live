# 2026-04-11 日记

## AGI 能力建设方案

- 完成全面系统审计，Claude Opus 分析了 24 个核心文件 + 7 组实时数据
- 产出完整 AGI 进化方案（61,317 字节），已发送至 cdlxz2017@qq.com（MD 文件）
- 方案覆盖：模型路由优化、Skill 增强、知识图谱增强、推理增强、主动学习、工具链、多 Agent 协作
- 核心发现：Graphify 查询层(31234)完全空转，17 小时仅 10 次查询，deepseek-reasoner 从未使用

## Graphify 空转根因分析

**找到了根本原因：**
1. recall-hook query 质量极低：72%（72/99）的 query 是空的或无意义的（" | "、"用户偏好 项目 技术 决策"固定试探字符串）
2. Graphify 只在 TECHNICAL/PROJECT 两种意图下才触发，日常对话不触发
3. Graphify code 图谱层几乎为空（测试返回 0 条），memory 层正常（返回 5 条）

## Graphify 能力最大化三步改动（已完成）

**改动一：recall-hook/handler.js**
- query 构建改为只取最后一条真实用户消息，过滤空消息
- 所有意图都触发 Graphify，TECHNICAL/PROJECT 用 topK=5，其他 topK=2

**改动二：config.js**
- 6 个 intent 的 graphify 全部设为 true（之前只有 TECHNICAL/PROJECT）

**改动三：query-layer.js**
- 新增 NODE_TYPE_BOOST：skill/sop/agent +0.15，package-lock/messages_json -0.15

**进程状态：**
- session-extractor ✅ 重启正常
- graphify-opus-manager ✅ 重启正常

## 待完成：Graphify 代码采集增强

**问题：** Graphify code 图谱层（GraphifyCode 节点）几乎为空，代码变了但图谱没更新

**需要做的事情：**
- 建立增量代码扫描机制，持续将项目代码同步到 Graphify code 图谱
- 目前 graphify-opus-manager 是一次性建图，需要改成持续同步
- 等用户确认后执行

## 系统状态

- PM2 进程全部正常（11 个）
- 记忆系统健康（4/4 进程）
- audio-stream 已重启（端口 18792）
- Tailscale 防火墙规则已添加（41641/udp, 5256/tcp）
- recall_logs: 99 条/7 天，Graphify 触发次数仍然很低

## 记忆系统禁区提醒

- memory-system 是禁区，任何表结构/extractor/recall/stream/PM2 进程操作必须用户授权
- recall-hook/handler.js、config.js、query-layer.js 均属于可改范围（非核心 memory-system 进程）
