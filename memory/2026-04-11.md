# 2026-04-11 日记

## AGI 能力建设方案

- 完成全面系统审计，Claude Opus 分析了 24 个核心文件 + 7 组实时数据
- 产出完整 AGI 进化方案（61,317 字节），已发送至 cdlxz2017@qq.com（MD 文件）
- 方案覆盖：模型路由优化、Skill 增强、知识图谱增强、推理增强、主动学习、工具链、多 Agent 协作
- 核心发现：Graphify 查询层(31234)完全空转，17 小时仅 10 次查询，deepseek-reasoner 从未使用

## Graphify 空转根因分析

**找到了根本原因：**
1. recall-hook query 质量极低：72%（72/99）的 query 是空的或无意义的（" | "、"用户偏好 项目 技术 决策"固定试探字符串）
2. Graphify 只在 TECHNICAL/PROJECT 两种意图下才触发，日常对话不触发
3. Graphify code 图谱层几乎为空（测试返回 0 条），memory 层正常（返回 5 条）

## Graphify 能力最大化三步改动（已完成）

**改动一：recall-hook/handler.js**
- query 构建改为只取最后一条真实用户消息，过滤空消息
- 所有意图都触发 Graphify，TECHNICAL/PROJECT 用 topK=5，其他 topK=2

**改动二：config.js**
- 6 个 intent 的 graphify 全部设为 true（之前只有 TECHNICAL/PROJECT）

**改动三：query-layer.js**
- 新增 NODE_TYPE_BOOST：skill/sop/agent +0.15，package-lock/messages_json -0.15

**进程状态：**
- session-extractor ✅ 重启正常
- graphify-opus-manager ✅ 重启正常

## 待完成：Graphify 代码采集增强

**问题：** Graphify code 图谱层（GraphifyCode 节点）几乎为空，代码变了但图谱没更新

**需要做的事情：**
- 建立增量代码扫描机制，持续将项目代码同步到 Graphify code 图谱
- 目前 graphify-opus-manager 是一次性建图，需要改成持续同步
- 等用户确认后执行

## 系统状态

- PM2 进程全部正常（11 个）
- 记忆系统健康（4/4 进程）
- audio-stream 已重启（端口 18792）
- Tailscale 防火墙规则已添加（41641/udp, 5256/tcp）
- recall_logs: 99 条/7 天，Graphify 触发次数仍然很低

## 记忆系统禁区提醒

- memory-system 是禁区，任何表结构/extractor/recall/stream/PM2 进程操作必须用户授权
- recall-hook/handler.js、config.js、query-layer.js 均属于可改范围（非核心 memory-system 进程）


# 2026-04-11 日记

## personal_memories 恢复写入（04:22-04:27）

**背景：** 3927 条 personal_memories 自 4月3日后停止写入，Claude Opus 审核方案发现 BUG（ON CONFLICT DO NOTHING 依赖不存在的 UNIQUE 约束），修正为幂等查询模式。

**执行内容：**
- 方案：PLAN-PERSONAL-MEMORIES-RESTORE.md（v1.1）
- 文件：summary-extractor.js saveSummary() 函数，第 207 行附近
- 逻辑：VALUABLE_CATEGORIES 过滤（排除 tech-doc），幂等写入 personal_memories
- PM2 重启完成，Redis 连接正常
- Git commit：0c8f076（方案）+ 2f2e3ea（代码）

**BUG 修复记录：**
- 原方案用 `ON CONFLICT DO NOTHING`，但 personal_memories 表不存在 source_ids 的 UNIQUE 约束，PostgreSQL 会报错
- 修正为幂等查询模式：先 SELECT 检查存在，再 INSERT

**发现的新 BUG：**
- VALUABLE_CATEGORIES 白名单与 memory_summaries.summary_type 完全不匹配
- memory_summaries 实际类型：factual/event/preference/instruction（209/85/10/5 条）
- 白名单里的：fact/pending/decision/plan（完全对不上！）
- 导致 360 条 summary 因类型不匹配被过滤掉
- **修复：** 白名单改为 memory_summaries 实际类型（factual/event/preference/instruction 等 10 种）
- Git commit：324db3a

## Graphify 对齐链路深度排查（04:33-04:55）

**根本原因分析（Claude Opus）：**
- `syncSummaryToNeo4j()` 只创建 PersonalMemory 节点，零对齐逻辑
- PersonalMemory 节点（1089 个）从未与 GraphifyCode 对齐
- `backfill-alignments.js` 一次性脚本，无定时任务
- `bridge-layer.js` 的 alignEntities() 只在文件变化时触发
- `cron-incremental-neo4j-sync.js` 未作为 PM2 进程运行，382 条 summary 未同步到 Neo4j

**已修复：**
1. summary-extractor.js 新增对齐逻辑（syncSummaryToNeo4j 末尾，~110 行）
2. cron-incremental-neo4j-sync.js 一次性同步 backlog（last_synced_id=480），加入 PM2 cron（每5分钟）

**未修复（待后续）：**
- cron-incremental-neo4j-sync.js 未加入 PM2 常驻进程（仅 cron 任务）

## Graphify 查询层问题（04:37-05:04）

**问题：查询总是返回空**

**根因：**
1. `queryGraphify()` 用 `CONTAINS 'memory system alignment'` 精确子串匹配，任何多词查询都为 0
2. `enrichWithAlignments()` 在 graphifyItems.length === 0 时直接返回，36,563 条 ALIGNED_TO 沉睡
3. GraphifyCode 节点无 code_content 字段，无法搜索实际代码

**方案对比：**
- C1（Neo4j全文索引）：解决 Neo4j 侧问题 → **推荐**
- C2（Embedding向量）：改动大，79k 节点需重新 embedding
- C3（PG全文检索）：解决 PostgreSQL 侧问题，无法激活 ALIGNED_TO → **不适用**

**C1 执行结果（Claude Opus）：**
- Neo4j 在线创建全文索引 `graphify_code_fulltext`（79,108 节点，零停机）
- query-layer.js：CONTAINS → CALL db.index.fulltext.queryNodes()
- `"memory system alignment"` 修复前返回空 → 修复后返回 3 条有效结果
- queryCache 命中率从 0% → 50%
- 任务标记完成：TASK-20260410-001 ✅

## OpenClaw 文档系统分析（DeepSeek-Reasoner，04:59）

**分析报告：** memory/DOC-ANALYSIS-OPENCLAW-SYSTEM.md

**8 个文件问题汇总：**
| 文件 | 关键问题 |
|------|---------|
| AGENTS.md | Session Startup 脚本路径错误；"memory-system是禁区"矛盾 |
| SOUL.md | target 硬编码（微信）；模型名称不一致 |
| USER.md | 完全空白 |
| TOOLS.md | 7天前过时路由；Claude Opus 未记录 |
| HEARTBEAT.md | 模板状态，无实际内容 |
| MEMORY.md | 技术细节淹没决策原则 |
| IDENTITY.md | 与 SOUL.md 重复 |
| BOOTSTRAP.md | 已删除（正常） |

**系统级矛盾：**
- AGENTS.md 说 memory-system 是禁区，但 MEMORY.md 说它是核心被管理对象
- 正确禁区定义：表结构/extractor/recall/stream/PM2 进程（不是整个 memory-system 目录）

**建议文件结构：**
```
SOUL.md（精简+技术映射）
USER.md（P0 立即填充）
AGENTS.md（重写+修正禁区定义）
TOOLS.md（更新模型路由）
HEARTBEAT.md（填入实际心跳任务）
MEMORY.md（清理技术细节）
memory/TECH-DETAILS.md（新增：技术细节）
```

**重写优先级：**
- P0：USER.md、AGENTS.md
- P1：SOUL.md、TOOLS.md
- P2：HEARTBEAT.md、MEMORY.md
- P3：IDENTITY.md（可并入 SOUL.md）

**任务已创建：** TASK-20260410-002（文档系统重写，P1）

## 今日 Git 提交记录

| Commit | 内容 |
|--------|------|
| 0c8f076 | 方案：personal_memories 恢复写入 |
| 2f2e3ea | 代码：summary-extractor.js personal_memories 幂等写入 |
| 324db3a | BUG修复：VALUABLE_CATEGORIES 修正 |
| 324db3a | cron-incremental-neo4j-sync backlog 同步 |
| [C1修复] | Neo4j 全文索引 + query-layer.js CONTAINS 修复 |
