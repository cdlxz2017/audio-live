# 2026-04-03 日志

## 今日重要事件

### Gateway 配置修改 SOP 建立

**背景**：用户要求建立严格的 Gateway 重启 SOP，以避免之前因配置修改导致的 Gateway 关闭问题。

**建立的 SOP 文件**：`SOP-GATEWAY-RESTART.md`

**SOP 核心要点**：
1. **阅读技术手册** — 修改前必须理解字段含义
2. **备份配置文件** — 带时间戳备份，位置：`/home/ai/.openclaw/openclaw.json.bak.{timestamp}`
3. **语法检查** — 使用 `jq empty` 验证 JSON
4. **修改配置** — 仅使用 `edit` 工具，禁止 `write`
5. **二次验证** — 确认变更符合预期
6. **重启 Gateway** — 使用 `gateway restart`
7. **紧急回滚流程** — 一键回滚命令已定义

**禁止事项（铁律）**：
- 禁止使用 `write` 工具修改配置
- 禁止无备份修改
- 禁止跳过语法检查
- 禁止跳过手册阅读
- 禁止不验证就启动

**exec 权限问题**：
- 用户要求开启所有 exec 工具访问权限
- 已修改配置：`tools.exec.allowFrom: ["*"]`
- 但 exec 仍报 `pairing required` 错误
- 可能需要控制 UI 中完成会话配对

**PostgreSQL 检查**：
- 用户提到有 PostgreSQL 存储历史记录
- 但配置文件中未找到数据库连接信息
- 无法确认数据库是否仍在运行

**系统状态**：
- Gateway 配置已更新，Telegram 已关闭
- 但 Gateway 需要重启才能生效
- 需要用户手动执行 `openclaw gateway restart`

## 待处理事项

- [ ] 完成 exec 工具配对
- [ ] 检查 PostgreSQL 数据库状态
- [ ] 完成记忆系统初始化（BOOTSTRAP.md 未删除）
- [ ] 验证 Gateway 重启后 Telegram 关闭是否生效
