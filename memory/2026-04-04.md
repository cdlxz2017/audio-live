

---

## 记忆系统全面梳理 v5.0（07:54 实测验证）

### 一、系统全景

```
外部渠道（微信/飞书/Telegram）
         │
         ▼
┌─────────────────────────────────┐
│  memory-extractor-hook ✅开启   │
│  session-capture-hook ✅注册    │
└──────────────┬──────────────────┘
               ▼
        Redis Stream: memory:messages
               │
               ▼
┌─────────────────────────────────┐
│  memory-daily-extractor (PM2)   │
│  消费 Stream → LLM 提取         │
│  → memories + personal_memories   │
└─────────────────────────────────┘

Session 文件（30秒准实时）
         │
         ▼
┌─────────────────────────────────┐
│  session-extractor-loop (PM2)    │
│  每30秒 → session-reader 配对   │
│  → conversation_messages          │
└─────────────────────────────────┘
```

### 二、数据表（5张）

| 表 | 行数 | 用途 | recall 查 |
|---|---|---|---|
| `conversation_messages` | 34 | 原始对话存档 | ❌ |
| `personal_memories` | 3927 | 用户记忆 content 直存 | ✅ 双表 |
| `memories` | 9 | 实体记忆 entity/attr/value | ✅ 双表 |
| `recall_logs` | 12 | 召回历史 | — |
| `memory_graph_sync` | 0 | Neo4j 同步追踪（事件驱动） | — |

### 三、核心脚本（14个）

**基础设施**
| 脚本 | 作用 |
|---|---|
| `config.js` | 所有配置（DB/Redis/LLM） |
| `db.js` | PostgreSQL 连接池 |
| `redis.js` | ioredis 5.x，xadd 已修复 |
| `embedder.js` | BGE-m3 向量生成（Ollama） |
| `cursor-store.js` | Redis Hash 断点续传 |
| `circuit-breaker.js` | LLM 熔断器 |

**写入管道**
| 脚本 | 触发 | 目标 |
|---|---|---|
| `session-sync.js` | 被 hook 调用 | → Redis Stream |
| `memory-daily-extractor.js` | PM2 持续消费 | → memories + personal_memories |
| `memory-writer.js` | 被 extractor 调用 | → memories |
| `extractor-file-based.js` | PM2 每30秒 | → conversation_messages |
| `session-reader.js` | 被 extractor 调用 | JSONL 解析+配对，已修复 thinking 过滤 |
| `memory-file-extractor.js` | 手动/cron | → personal_memories |

**召回管道**
| 脚本 | 作用 |
|---|---|
| `session-recall.js` | 双表向量搜索 → prompt 注入 |
| `session-context-loader.js` | 新 session 加载上下文 |

**工具脚本**
| 脚本 | 作用 |
|---|---|
| `backfill-personal-memories-embeddings.js` | 补全缺失 embedding |
| `backfill-personal-memories.js` | Neo4j 批量回填 |

**PM2 进程**
| 进程 | 间隔 | 状态 |
|---|---|---|
| `memory-extractor` | 持续运行 | ✅ running |
| `session-extractor` | 30秒 | ✅ running |

### 四、端到端实测结果（07:54）

| 步骤 | 结果 |
|---|---|
| 写入 `memories` 表 | ✅ id=1885011 |
| 召回"记忆系统检修" | ✅ 5条，personal_memories 占4条 |
| 召回"AGI进化方案" | ✅ 3条，第一条就是决策本身 |
| recall_log 记录 | ✅ 81ms < 150ms 预算 |
| Redis Stream | ✅ 0（无堆积） |
| 清理测试数据 | ✅ 已删除 |

### 五、召回实测输出

**AGI进化方案召回（精准）**：
```
personal_memories | decision | 决策：采用AGI进化五个核心方案
personal_memories | technical | 协调层统一管理
personal_memories | technical | 方案二输出insight喂给方案五
```

**"记忆系统检修"召回（双表生效）**：
```
personal_memories | decision | 测试整套记忆系统...
personal_memories | decision | 用户决定更新并完善记忆系统...
personal_memories | plan | 用户计划建立每日固定时间检测...
personal_memories | technical | 当前配置支持多种功能...
personal_memories | fact | 主人希望构建本地记忆系统...
```

### 六、已知问题状态

| # | 问题 | 状态 |
|---|---|---|
| 1 | personal_memories 未接入 recall | ✅ 已修复（双表并行） |
| 2 | recall_log UUID 报错 | ✅ 已修复 |
| 3 | AGI 方案7条缺 embedding | ✅ 已补全 |
| 4 | memories.content NULL | ✅ 无影响 |
| 5 | memory_graph_sync 空 | ✅ 事件驱动设计 |
| 6 | session-capture-hook 对 webchat 无效 | ✅ 30秒轮询补偿 |
| 7 | xadd ioredis 5.x SIGKILL | ✅ 已修复 |
| 8 | memories memory_type CHECK | ⚠️ 仅 factual/event 可写 |

### 七、维护命令

```bash
# 查看进程
pm2 list | grep -E "memory|session"

# 查看日志
pm2 logs memory-extractor --lines 20 --nostream
pm2 logs session-extractor --lines 20 --nostream

# 手动跑 extractor
node /home/ai/.openclaw/workspace/memory-system/scripts/extractor-file-based.js

# 测试召回
node -e "
const {recallService}=require('./scripts/session-recall');
recallService.recall({tenantId:'00000000-0000-0000-0000-000000000000',userId:'cc530e44-a66c-43b0-8302-2b26b879f856',query:'AGI进化方案',topK:3})
  .then(r=>{r.forEach(m=>console.log(m._table,m.entity,m.value));process.exit(0);});
"

# 补全 embedding
node /home/ai/.openclaw/workspace/memory-system/scripts/backfill-personal-memories-embeddings.js
```

### 八、架构文档
- 位置：memory-system/ARCHITECTURE.md（v5.0，含全景图/脚本清单/维护命令/修复历史）

---

## 📌 Session 交接备忘（15:33）

用户要求新建 session 继续工作。以下是当前状态总结：

### 项目全貌

| 文档 | 路径 | 内容 |
|------|------|------|
| 完整设计方案 | `memory-system/MEMORY-SYSTEM-DESIGN.md` | v4.0 架构（双通道、召回引擎、提取管道、Neo4j、多租户等） |
| 会话存储改造 | `memory-system/SESSION-STORAGE-PLAN.md` | v4.1 改造（cursor 机制、安全 JSONL 读取、回填策略） |
| 开发任务清单 | `memory-system/TASKS.md` | 全部完成（v4.0 Phase 1-8 + v4.1 Phase 1-3） |
| 架构实测文档 | `memory-system/ARCHITECTURE.md` | v5.0 含全景图/脚本清单/维护命令/修复历史 |
| 长期记忆 | `MEMORY.md` | 系统配置、数据库连接、数据资产、项目状态 |

### 当前运行状态

- ✅ **记忆系统 v4.1** — 已部署运行
- ✅ **PM2 进程**：memory-extractor（持续消费）+ session-extractor（30秒轮询）
- ✅ **Neo4j graph-linker** — systemd 服务运行中
- ✅ **recall-hook** — before_prompt_build 召回注入正常
- ✅ **extractor-hook** — 已禁用（被 file-based extractor 取代）
- ✅ **personal_memories → Neo4j 回填** — 已完成（463 条有价值记忆）

### 新 session 启动时需要的上下文

1. 读 `MEMORY.md`（长期记忆）
2. 读 `memory/2026-04-04.md`（今日笔记，含系统全景）
3. 如需修改系统 → 读 `memory-system/TASKS.md` 了解任务状态
4. 如需了解架构 → 读 `memory-system/ARCHITECTURE.md`
5. 如需了解设计决策 → 读 `memory-system/MEMORY-SYSTEM-DESIGN.md`

## lingyi-crm Git仓库建立

- 仓库路径：`/home/ai/.openclaw/workspace/projects/lingyi-cms/`
- 提交了 SYSTEM-REVIEW.md 和所有后端代码（30个文件）
- 新增 CHANGELOG.md 记录每次修改
- 修改后记得：git add . && git commit -m "描述"
